# Ignore node_modules (this is very important)
node_modules/

# Ignore lockfiles other than npm's package-lock.json
yarn.lock
pnpm-lock.yaml

# Ignore .env files (to prevent leaking sensitive info)
.env

# Ignore system files
.DS_Store

